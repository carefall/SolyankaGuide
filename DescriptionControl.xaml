<UserControl x:Class="SolyankaGuide.DescriptionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolyankaGuide"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#99000000" CornerRadius="10" Padding="20" Margin="15,15,5,15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image x:Name="DescImage" VerticalAlignment="Top" Grid.Row="0"/>
            <TextBlock x:Name="DescHeader" Grid.Row="1" HorizontalAlignment="Stretch" Foreground="Yellow" FontFamily="Segoe UI, Noto Sans, Arial, Times New Roman" FontSize="24" TextWrapping="Wrap" TextAlignment="Center" FontWeight="Bold"/>
            <ScrollViewer x:Name="DescScrollView" Grid.Row="2" FontFamily="Segoe UI, Noto Sans, Arial, Times New Roman" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>

<UserControl x:Class="SolyankaGuide.GriddedDescription"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolyankaGuide"
             mc:Ignorable="d" 
             VerticalAlignment="Top"
             Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=WrapPanel}, Converter={StaticResource ThreeColumnConverter}}"
             Height="{Binding Width, RelativeSource={RelativeSource Self}, Converter={StaticResource GriddedRatioConverter}}">
    <Grid x:Name="DescGrid">
        <Image x:Name="Image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="Fill" SizeChanged="Image_SizeChanged"/>
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="#99000000">
            <TextBlock x:Name="TileName" TextWrapping="Wrap" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextAlignment="Center" FontFamily="Segoe UI, Noto Sans, Arial, Times New Roman" FontSize="20" FontWeight="Bold" Foreground="Yellow" Margin="0,0,0,7"/>
        </Border>
    </Grid>
</UserControl>
